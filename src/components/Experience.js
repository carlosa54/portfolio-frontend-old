import React, { Component} from 'react';

const Experience = (props) => {
	return (
		<h1 style={{textAlign:'center', verticalAlign: 'middle'}}>This is the Experience component</h1>
	);
}

export default Experience
